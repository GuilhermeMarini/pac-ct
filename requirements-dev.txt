# Development-only dependencies. `app.py` bootstraps requirements.txt only --
# nothing here is imported in production.
pytest>=8.0
# Lint and types. Nothing imports these -- they run in CI and by hand:
#     .venv/bin/python -m ruff check .
#     .venv/bin/python -m mypy
ruff>=0.6
mypy>=1.11
